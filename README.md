this just for simple test where I could play with and debug the transaction managers

created teble query 

CREATE TABLE JTA_INV (
                           productId VARCHAR(10) PRIMARY KEY,
                           balance INT
);
